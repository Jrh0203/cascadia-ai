Continue executing the full active R2-MAP goal from current authoritative john2 remote-storage state. Do not summarize and stop while safe in-scope work remains. Maintain root as control-plane only with one owner agent each for john1, john2, and john3, never john4. Preserve every scientific, identity, recovery, exhaustive-action, memory, zero-swap, dashboard, storage-transport, and phase-barrier gate. Reconcile prior turn outputs before acting. The canonical state restarted at revision 0 contracts-ready; legacy W0-W6 hashes are provenance only, so revalidate W0-W6 under the new transport and v1.1 source before W7. The only authoritative campaign endpoint is john2:/Users/john2/cascadia-bench/r2-map-v1 through the strict hash-verified remote-storage transport. Never touch the immutable legacy /Volumes/John_1/cascadia-cluster/r2-map-v1 tree, never use SSHFS/tree synchronization, and never use the retired nested APFS lifecycle. Keep John1 free of campaign data/build/cache/temp/checkpoint/log/replay artifacts except source code, one disposable dashboard projection of at most 64 KiB, and during generation one registered /private/tmp runtime containing exactly a John2-built signed/hash-verified arm64 executable plus <=64-KiB manifest, <=64 MiB combined, mode 0700/0500, with no local output and mandatory cleanup receipt. Only publish the registered terminal sentinel remotely when the complete Definition of done is proven or a genuine authorization-only blocker remains.
