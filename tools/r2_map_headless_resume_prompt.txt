Resume the previously blocked R2-MAP persistent goal and execute it headlessly. The desktop-parent zombie-process blocker is avoided because this run is owned by a detached CLI process. Keep the full scientific objective unchanged: finish W0-W7, the first complete expert-iteration cycle, and the no-pruning performance program under AGENTS.md and the authoritative plan. Root remains control-plane only; maintain exactly one owner agent each for john1, john2, and john3; never use john4. Work from the current dirty shared tree and preserve user changes. The canonical john2 campaign intentionally restarted at revision 0 contracts-ready; legacy W0-W6 hashes are provenance only, so revalidate every W0-W6 gate under the new transport and v1.1 source and do not launch W7 until all are genuinely green. The later user storage directive supersedes every earlier SSD/APFS directive: the sole authoritative campaign endpoint is host john2 at /Users/john2/cascadia-bench/r2-map-v1, accessed from other hosts only through the strict hash-verified remote-storage transport. Never mount it with SSHFS and never synchronize a campaign tree. The former /Volumes/John_1/cascadia-cluster/r2-map-v1 tree is immutable legacy evidence: do not write, migrate, rename, or delete anything there. John1 remains the MLX compute host and may retain only source code, one disposable dashboard projection of at most 64 KiB, and during its generation phase one registered /private/tmp runtime containing exactly a John2-built signed/hash-verified arm64 executable plus <=64-KiB manifest, <=64 MiB combined, mode 0700/0500, with no local output and mandatory cleanup receipt. Stream bounded training inputs into memory and atomically publish checkpoints, logs, caches, temporary products, replays, and all other campaign artifacts to john2. The retired nested APFS lifecycle must not be used. Keep the dashboard current, including the John1 runtime verification and cleanup identity.

Continue autonomously across turns. Before ending every turn, leave the work resumable. Only when the full goal is proven complete, atomically publish control/headless-terminal.json at the authoritative john2 endpoint with status "complete" and evidence identifiers through the strict remote-storage transport. If and only if a genuine authorization-only blocker remains after exhausting safe in-scope alternatives, publish the same remote object with status "authorization-blocked" and exact required user action. Never write a terminal sentinel for ordinary incomplete work, test failures, resource pressure, or complexity.
