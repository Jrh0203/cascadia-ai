# Cascadia V2 Roadmap

## Plan To 100 Restart

- [x] Complete the exact R2-MAP bootstrap, protected blinded smoke, and fixed
      250-pair cross-architecture gate. Reject R2-MAP at 91.604 versus 97.468
      for exact NNUE (paired delta -5.864, 95% CI [-6.414, -5.314]); do not
      start expert iteration and return primary research to NNUE.
- [x] Replace host/parity partitions with the 9/10/10 topology-free Bacalhau
      fabric and durable scheduler-capacity admission backpressure.

- [x] Freeze the exact K32/R600 full-terminal reference and 10x threshold.
- [x] Reach 10.0045x with exact shared-memory MLX, native hot-path work
      reduction, parent-afterstate feature contexts, direct rollout-template
      preparation, bounded pipeline state slices, dead local outcome-buffer
      elision, and fresh PGO.
- [x] Audit exact opponent greedy decision reuse and reject it at a measured
      0.512% reuse rate before adding production synchronization overhead.
- [x] Implement and exhaustively validate exact Card A wildlife score
      contexts; reject and remove them after a 0.573% source gain missed the
      preregistered 1.00% promotion floor.
- [x] Test exact MLX sparse-row locality ordering; reject and remove it after
      host sorting/scatter cost produced a 6.873% crossed source regression.
- [x] Remove exact-public-state grouping and duplicate candidate-cache work
      after measuring only 12 reusable templates among 440,239 requests;
      accept a 4.128% fresh-PGO gain on both workers.
- [x] Sweep larger exact MLX pipeline cohorts; reject all 128-256-state
      treatments after 4.78-5.10% two-host regressions and large RSS growth.
- [x] Replace full-population preparation and opponent-advance scans with
      exact bounded cohort slices; accept a 1.167% fresh-PGO gain on both
      workers.
- [x] Test single-owner validation for private shared CSR requests; reject and
      remove it after a 0.580% crossed wall-time regression despite materially
      lower decode/validation time.
- [x] Filter qualified greedy wildlife scans to market-represented categories;
      reject and remove it after john3 regressed and allocator footprint rose,
      despite a 0.648% combined source gain.
- [x] Elide the qualified greedy `placed_tiles` snapshot with direct indexed
      traversal; reject and remove it after a 0.066% combined source gain,
      a 0.330% john3 regression, and 4.021% allocator-footprint growth.
- [x] Combine exact candidate-placement capacity reservation with explicit
      total-order unstable sorting; reject and remove it after added tie
      comparisons regressed template preparation 2.733-3.550%.
- [x] Isolate exact placement-vector capacity reservation with stable sorting;
      reject and remove it after john2 regressed 0.941% and the combined
      source result regressed 0.235% despite lower targeted-stage work.
- [x] Keep one exact evaluator worker alive across all halving rounds; reject
      and remove it after fresh PGO regressed john3 by 0.714% and remained
      59.847 ms above the 10x threshold despite an 80% worker-identity
      reduction and positive source screen.
- [x] Pack candidate-placement metadata from eight bytes to four while
      preserving stable ordering; reject and remove it after source timing
      regressed john2 by 0.631%, john3 by 0.116%, and 0.375% combined despite
      reducing template preparation on both workers.
- [x] Elide the dead local rotation-invariant outcome buffer from the shared
      production candidate path; accept a 1.014% fresh-PGO gain and clear the
      10x single-Mac gate at 14.096347 seconds, or 10.0045x.
- [x] Clear the 10x single-Mac gate without changing strength.
- [x] Freeze the new full-legal audit teacher at 242.433050 seconds with its
      own 24.243305-second 10x threshold.
- [x] Accept exact 4,096-row static-screen cohorts and collision-checked
      sparse-row deduplication, reducing the audit to 212.191376 seconds.
- [x] Cache exact paid-screen public states within each diagnostic; improve
      both workers by more than 34% and reduce the complete audit to
      177.686057 seconds with exact semantic parity.
- [x] Measure independent realized-hidden process concurrency; reject it after
      two processes reached only 1.162-1.165x throughput and four reached
      1.399-1.401x on both workers despite exact results and zero swaps.
- [x] Reuse exact game-scoped public decisions across realized-hidden champion
      continuations and outer play; accept an 8.576% crossed gain and reduce
      switch-free production to 162.045309 seconds.
- [x] Advance eight realized-hidden finalists through one deterministic
      scheduler and MLX evaluator; accept a 15.391% crossed gain and reduce
      switch-free production to 143.775461 seconds.
- [x] Measure exact per-search sparse-row recurrence across waves and halving
      rounds; reject prediction caching after 6,116,501 observed rows produced
      zero repeats.
- [x] Measure exact row duplication across contemporaneous multiplexed
      requests; reject global deduplication after only 247 of 891,486 rows
      repeated, a 0.027707% rate.
- [x] Replicate exact stage profiles on john1, john2, and john3. Serialized
      MLX evaluation consumes 47.813 seconds and 37.123% of remote wall;
      opponent advancement and rollout-template preparation consume 21.258%
      and 18.867% of cumulative Rust intervals.
- [x] Sweep exact H1 vector widths 4/8/16 in a three-node Latin square;
      reject and remove wider kernels after width 8 regressed combined MLX
      evaluation 4.720% and width 16 regressed it 15.210%.
- [x] Test exact SIMD-group sharing of H1 CSR offsets and indices; reject and
      remove it after combined wall improved only 0.180% and MLX evaluation
      only 0.711%, with a john1 regression.
- [x] Replicate exact H1, H2, and output timing across all three Macs. H1
      dominates at 76.410-78.900% of corrected layer time; john2 and john3
      agree within 0.081 percentage point, and the H1 boundary moves 183.927
      GB across its global write and read.
- [x] Test exact threadgroup-resident H1-to-H2 fusion with unchanged H1
      feature order and H2 input order; reject and remove the scalar-H2
      geometry after combined wall regressed 1.223% and MLX evaluation
      regressed 4.193% across three slower nodes.
- [x] Close the production-H2-geometry fusion after direct parity and before
      an authorized screen when the project owner declared the
      143.775461-second exact teacher baseline sufficient. Retain an
      inadvertently launched post-closure screen only as invalid provenance.
- [x] Declare the 1.686192x exact full-legal teacher improvement a performance
      win and retire the remaining 5.931x target as a strength-campaign
      blocker.
- [x] Run the full-legal decision-regret audit; complete 1,040 decisions and
      3,872,079 actions, measure 6.995 points of first-order headroom, identify
      proposal/frontier regret as dominant, and reject K64 at 89.904% recall.
- [x] Complete the separately locked K1024 screen-width recovery. Across 13
      paired games and 1,040 decisions, K1024 recalled 99.423% of measured
      winners and retained only 0.002 mean regret, passing both substantive
      gates.
- [x] Run the corrected 12-game, three-node Full-Legal Public Oracle V1 pilot.
      It passed every integrity gate and improved 11 of 12 games, but stopped
      at 98.583 mean and +2.375 paired points, below both advance thresholds.
      Its confirmation domain remains unopened.
- [x] Run the preregistered K1024 online oracle pilot on fresh seeds
      `62040-62051`. It passed every integrity gate and achieved 99.375%
      top-screen recall, but stopped at 98.417 mean and +2.854 paired points.
- [x] Close the sealed K1024 confirmation as not authorized. Both pilot score
      gates failed, so K2048 and further brute-force width are not opened.
- [x] Build and evaluate `complete-action-graded-oracle-ranker-v1` across
      three frozen MLX replicas and a cross-host matrix. Reject it before
      sealed test after the selected replica reached 0.090184 retained regret
      but only 73.33% top-64 exact-winner recall; close ADRs 0082 and 0083
      unopened.
- [x] Audit R4800 target identifiability on train and validation under ADR
      0086. Only 31.67% of validation winners were distinguishable, but model
      top-64 confidence-set coverage was only 86.25% and distinguishable-winner
      recall only 85.53%, classifying observable representation or
      optimization as still material.
- [x] Complete ADR 0087's R1200-cohort target-sufficiency audit. Reject the
      proposed fixed-architecture learner before training after its oracle
      upper bound reached only 97.08% confidence-set coverage and 90.79%
      distinguishable-winner recall, with early and middle below gate.
- [x] Execute ADR 0088's paired three-node local-geometry ranker. Reject it
      before sealed test after the selected john2 replica reached 0.093757
      retained regret but only 74.17% exact recall and 87.92% confidence-set
      coverage; all cross-host and performance checks passed.
- [x] Execute ADR 0089's frontier-anchored set ranker across john1-john4.
      Reject it before sealed test after the selected john2 replica reached
      76.67% exact recall, 90.42% confidence coverage, and only 26.21%
      nonfrontier target-positive recall despite a 99.58% target ceiling and
      bit-identical cross-host replay.
- [x] Run the four-way independent failure-classification fork: train-fit,
      observable-collision, objective-gradient, and error-anatomy audits.
      Select optimization/capacity underfit after train target recall reached
      only 29.36%, with zero exact observable collisions and broadly
      distributed errors.
- [x] Train one single-host target-only curriculum pilot while john3 and john4
      run independent reachability and trajectory audits. Reject it after
      selected validation target recall reached only 26.29% and train recall
      only 30.97%, despite a perfect ±6 score-range ceiling.
- [x] Replace uniform set cross entropy with a directly top-K-calibrated
      smooth extreme-value boundary objective in one single-host pilot.
      Reject it after no trained epoch beat the warm start even though direct
      bounded score optimization recovered every audited target set.
- [x] Test a rank-matched full-boundary objective that pairs every target slot
      with a corresponding hard nontarget. Reject it after no epoch beat the
      warm start despite exact gradient coverage and a perfect bounded
      score-space audit.
- [x] Audit frozen candidate-embedding separability with a small probe. Reject
      head-only work after linear and nonlinear probes reached only 22.48%
      and 24.67% train target recall with zero exact sets.
- [x] Audit a frozen-trunk raw-observable bypass. Reject candidate-wise
      bypasses after raw linear, raw nonlinear, and embedding-plus-raw probes
      reached only 26.31%, 28.35%, and 30.50% train target recall.
- [x] Compare distinct linear-memory candidate-relative trunk mechanisms
      across the four Macs. Reject all four after train recall remained
      between 28.61% and 29.07%, classifying the 192-dimensional pre-pool
      candidate projection itself as insufficient.
- [x] Compare distinct pre-compression candidate-factor integration
      architectures across the four Macs. Reject all four after train recall
      remained between 29.39% and 30.88%, classifying the seven projected
      candidate factors themselves as insufficient.
- [x] Execute ADR 0098's four-way raw factor-construction fork. Reject all
      four after train recall ranged from 17.95% to 37.87% and no arm recovered
      one complete train target set, closing further neural construction,
      head, pooling, width, and optimizer work on the hard binary target.
- [x] Audit finite-R1200 top-64 label stability and alternative supervision
      ceilings across four distinct CPU experiments. Reject the hard labels
      after only 41.20% resampling recall and 2.50% exact-set reproduction,
      while accepting expected-rank supervision after its validation ceiling
      retained every R4800 winner and confidence set at zero regret.
- [x] Train one frozen uncertainty-aware expected-rank MLX pilot on one Mac
      while the other three Macs run independent target, optimization, and
      generalization diagnostics. Reject scale 64 after selected train and
      validation recall reached only 32.21% and 27.81%; identify target
      dilution after only about 45% of probability mass landed inside the
      deployed set.
- [x] Run one preregistered deployment-aligned scale-16 expected-rank pilot on
      a single trainer while the other Macs execute nonduplicative signal,
      trajectory, and error-anatomy work. Reject it after selected train
      recall reached only 30.23%, below ADR 0100 and the material-fit gate,
      despite 93.76% deployed-set target mass and passing optimization audits.
- [x] Run a four-host expected-rank fit-scaling and gradient-interference
      audit. Local recovery failed even at one group, width scaling was
      immaterial, and gradient conflict lacked empirical interference,
      classifying the failure as local optimization or representation
      insufficiency.
- [x] Audit the exact box-constrained free-residual optimum, direct
      free-residual optimization trajectory, and deployed top-64 selector on
      the frozen local cohort. The objective recovered every target set and
      frozen AdamW remained underfit, but the projected numerical control
      narrowly missed its preregistered precision gate, invalidating treatment
      selection.
- [x] Repair only ADR 0103's projected numerical-control gate with a
      higher-iteration preregistered solver and cross-host replay. The
      decision-tolerance KKT and objective-gap gates passed, but strict
      convergence failed on 12 groups and three selector outcomes disagreed,
      so ADR 0104 closed invalid.
- [x] Independently reconstruct the frozen 24-group optimum and selector with
      arbitrary-precision arithmetic and a fine-grained dynamic cross-host
      queue. ADR 0105 exposed an invalid integer conversion of fractional
      expected ranks; its 48 tasks otherwise completed in 5.97 seconds with
      bit-identical replays and zero queued-work idle.
- [x] Replay the arbitrary-precision active-set control with exact
      `Decimal.from_float` rank conversion. ADR 0106 recovered 100% of all 24
      target sets, matched the frozen objective within `1.89e-14`, and
      classified frozen optimizer hyperparameters as insufficient.
- [x] Test one analytically calibrated local optimizer mechanism on frozen
      free residuals. Its strength gate passed at 96.59% recall and 79.17%
      exact sets, but five groups saturated before 1,200 accepted updates,
      invalidating Stage 1 and keeping neural work closed.
- [x] Repair only the calibrated optimizer's numerical stop rule on groups
      0, 2, 8, 14, and 23, replay them cross-host, and recombine with the
      frozen 19 completed groups. ADR 0108 passed every gate at 96.59% recall
      and 79.17% exact sets with zero queued-work idle.
- [x] Run the authorized four-group calibrated monotone AdamW neural Stage 2
      with four cross-host replays. ADR 0109 closed pipeline-invalid after
      group 2 reproducibly failed the completion rule; terminal descriptive
      recall was 32.39% with zero exact sets.
- [x] Audit the retained ADR 0109 finite failure without additional neural
      training, identify the exact rejected convergence subcondition, and
      classify whether optimizer bookkeeping or full-model direction blocks
      the local mechanism. ADR 0110 proved a below-minimum-rate bookkeeping
      conflict and classified the corrected pipeline as public-observable
      representation insufficient.
- [x] Run ADR 0111, the single preregistered public-observable representation
      treatment, as four distinct local-geometry adapter origins across
      john1-john4 with cross-host confirmation. The valid pipeline reached
      71.13% recall and 25% exact sets, classifying the adapter insufficient.
- [x] Audit ADR 0111 bounded feasibility and exact observable aliases without
      training. ADR 0112 found 11,087 unique rows, no saturated residuals, and
      a 100%/100% bounded interval ceiling, classifying shared fit or optimizer
      insufficiency.
- [x] Run one same-representation control separating expected-rank gradient
      dilution from adapter capacity. ADR 0113 reached 59.86% recall and 25%
      exact sets; only the 324-candidate group fit, closing the shared adapter.
- [x] Reframe complete-action proposal learning around scalable hierarchical
      retrieval. ADR 0114 conditional-wide reached 99.18% validation recall,
      95% exact sets, 100% winner retention, and 482 mean proposals.
- [x] Build one learned `16 / 32 / 8` conditional draft, tile, and wildlife
      retrieval pilot and test it against the frozen structural ceiling.
- [x] Test the ADR 0116 boundary-only conditional tile objective. It improved
      validation tile recall from 66.57% to 70.59% but failed the proposal
      gate, closing this objective.
- [x] Audit conditional tile capacity and query-conditioned representation.
      ADR 0117 fit 16 and 256 hard-query cohorts exactly with the unchanged
      ranker, closing local capacity and selecting full-data exposure.
- [x] Use the three nontraining Macs for ADR 0119 successor forensics while
      ADR 0118 trains. Close target-mass resampling, prove 5.64-9.42x
      cross-stage score-scale mismatch, and prove that fixed oracle-factor
      aggregation recovers only 74.72% of validation targets at top 64.
- [x] Run one unchanged full-cache target-only tile origin with a larger
      preregistered epoch budget. ADR 0118 reached 99.80% train recall but
      regressed to 67.75% validation recall, closing uniform exposure.
- [x] Run ADR 0120's one frozen late cosine optimizer-schedule treatment.
      Reject it after 100% train recall paired with only 68.0% validation
      recall, closing further exposure, sampling, and schedule variants for
      this conditional pointwise tile ranker.
- [x] Use john1, john3, and john4 for ADR 0121 generalization forensics during
      the sole ADR 0120 origin. Rule out exact aliases and broad covariate
      shift; prove late-fit margin specialization and preselect structural
      regularization if ADR 0120 fails.
- [x] Attribute the ADR 0121 specialization across tile-item feature blocks.
      ADR 0122 selected local geometry at +0.2446 contribution, 0.1390 above
      descendant summaries, fixing the contingent regularization target.
- [x] Calibrate deterministic local-geometry corruption. ADR 0123 selected
      50% as the only rate that removed at least one quarter of the gap while
      keeping validation damage below two points.
- [x] Complete the one authorized ADR 0124 local-geometry dropout origin,
      cross-host replay, mixed ceiling, integrated proposal evaluation, and
      mechanical classification. Reject it as
      `local_geometry_dropout_tile_insufficient`.
- [x] Complete F1-F4: historical feature activation, exact footprint census,
      exact D6 transforms, and the adversarial information-preservation suite.
- [x] Complete R0's source-frozen extraction screen across 60,000 rows and
      twelve independent processes. Keep exact occupied entities after every
      dense compact arm missed the 1.5x state-build gate.
- [x] Complete R0's separate iso-architecture MLX tournament. All three dense
      compact arms were value-noninferior, but the best reached only 0.291x
      exact inference and 0.297x exact training throughput. Keep exact
      entities and select no R1 dense reinvestment arm.
- [x] Complete F5 corrected-schema foundation gates 1-13, including immutable
      champion migration, independent binary audit, and bit-identical
      Rust/MLX production parity.
- [x] Run F5's 200,000-state C0/T1 parity campaign and record the historical
      corpus activation counts. Record 200,000 exact matches, zero mismatches,
      and no corrected-tail activations; open no fine-tuning treatment.
- [x] Build and run F5's separate source-frozen corrected-schema activation
      census. All 301 corrected channels activated over 81,920 public states;
      retain low-rate fine-tuning as backfill behind the relational program.
- [x] Complete S1 exact semantic supply and refill-distribution foundations
      over 40,000 positions and 75 canonical archetypes.
- [x] Run S1's matched learned semantic-supply comparison. Preserve the
      aggregate candidate-relational signal but reject promotion after
      low-supply and independent-draft regressions.
- [x] Complete R2's exact sparse occupied, frontier, component, and motif
      foundation over 60,000 positions; authorize the matched MLX tournament.
- [x] Complete R2's matched MLX tournament and select the fixed-latent
      Perceiver as the only arm passing every frozen R0 value gate.
- [x] Complete R3's exact action-edit foundation and launch the matched
      full-afterstate versus radius-3, radius-2, and radius-1 MLX comparison.
- [x] Complete S4's exact candidate-relation foundation. Select 256 observable
      anchors after they reached 100% confidence coverage, 0.02874 regret,
      100% winner linkage, and 98.37% complete-candidate linkage in the
      corrected source-frozen rerun.
- [x] Build and qualify the shared S4 candidate-context cache. Bind all
      1,140,215 cached candidates to the R3 source order and prove
      byte-identical forward and reverse merges; fan out and whole-tree verify
      the merged production cache on john2, john3, and john4.
- [x] Complete and classify R3's matched MLX comparison. Reject all three
      compact arms under `r3_action_edit_mlx_all_treatments_degraded`; retain
      exact sparse full-R2 afterstates as the independent-candidate baseline.
- [x] Run and classify S4's 256-anchor independent, inducing-context,
      relation-segment, and combined neural comparison. Reject all treatments
      under `s4_candidate_context_mlx_all_treatments_degraded`; preserve the
      exact-relation slice signal but close this compact-rescue architecture.
- [ ] Execute R2-R6 sparse, action-centric, multi-resolution, quotient, and
      hybrid representation tournaments under the frozen promotion gates.
- [ ] Build and qualify complete-action MLX policy/value learning.
- [ ] Build stochastic tree search and expert iteration.
- [ ] Reach a fresh frozen 1,000-game mean of at least 100.000.

## Phase 0: Evidence

- [x] Adopt authoritative goal.
- [x] Create v2 branch.
- [x] Inspect and clear stale compute.
- [x] Record hardware/toolchain.
- [x] Audit v1.
- [x] Define architecture and benchmark contract.
- [x] Establish project-owned MLX environment and run Metal smoke/performance.
- [x] Build canonical v1 adapter and reproduce baseline.
- [x] Record fresh v2 random and greedy correctness/performance artifacts.
- [x] Record a timed, explicitly non-canonical v1 champion reference artifact.

## Phase 1: Deterministic Game

- [x] Create independent v2 workspace crates.
- [x] Define canonical domain types and `TurnAction`.
- [x] Implement pure validation and transactional apply/undo.
- [x] Implement deterministic setup and separated RNG streams.
- [x] Implement stable snapshots, replay, and hashing.
- [x] Port independently verified AAAAA scoring.
- [x] Add fixtures, property tests, and v1 differential tests.

## Phase 2: Simulation And Evaluation

- [x] Implement random/greedy baselines and evaluate a pattern-aware pilot.
- [x] Implement symmetric parallel match runner.
- [x] Implement seed-suite artifacts and game-block statistics.
- [x] Add paired strategy-comparison statistics.
- [x] Add deterministic criterion benchmark targets.
- [x] Record uncontended criterion measurements on this machine.
- [x] Generate machine-readable reports.
- [x] Generate Markdown reports from the same result artifact.

## Phase 3: Web Product

- [x] Create typed API application layer.
- [x] Build standalone responsive frontend.
- [x] Implement setup, play, undo/redo, save/load, analysis, and accessibility.
- [x] Add browser tests and desktop/mobile visual verification.

## Phase 4: MLX Foundation

- [x] Create `cascadia_mlx` package and one-command environment setup.
- [x] Define dataset/model/run manifests and sharded data format.
- [x] Implement encoders, model service, training, validation, and resumption.
- [x] Train fresh policy/value baselines on local hardware.

## Phase 5-6: Strength Campaign

- [x] Quantify candidate recall and score-loss sources.
- [x] Run registered architecture, search, distillation, and self-play studies.
- [x] Audit R600 teacher winner identifiability before another distillation
      run; close exact-action imitation after only 18.4% of validation winners
      cleared a 95% difference test.
- [x] Test same-budget common-random-number sequential halving under ADR 0071;
      pilot passed at +1.167 and 97.083 mean.
- [x] Confirm CRN over 20 fresh paired games under ADR 0072; rejected at
      -0.363 with 95% CI `[-1.129,+0.404]`.
- [x] Test explicit hex adjacency and oriented terrain-edge message passing
      under ADR 0073; rejected on fresh validation after correlation and MAE
      regressed despite a small pairwise log-loss improvement.
- [x] Audit repeated counterfactual public-state returns under ADR 0074; R8
      was stable and locally affordable, but absolute R16 state means failed
      the frozen signal-width gate.
- [x] Audit nearest-neighbor same-decision action advantages under ADR 0075;
      R8 was stable and accurate, but the top-four R16 range missed the frozen
      1.50-point width gate at 1.367.
- [x] Audit rank-stratified counterfactual contrasts across the existing H6
      frontier; width passed at 2.803 points, but R8 exact winner agreement
      missed its frozen gate by one of 32 groups.
- [x] Qualify an R12 rank-stratified shared-seed estimator on fresh validation
      games; all gates passed at 78.13% exact winner agreement and 0.037 mean
      regret.
- [x] Diagnose and permanently correct ADR 0078's finite-market collection
      failure; archive the unconditioned corpus, version deterministic
      rejection conditioning in the teacher manifest, and replay the exact
      failing game plus the full MLX smoke.
- [x] Complete the corrected ADR 0078 collection of the authorized 128-game
      train and 32-game validation R12 corpus on the local john1/john2
      cluster.
- [x] Train the implemented MLX public-supply-aware complete-candidate-set
      ranker and apply every frozen validation gate; rejected after six gates
      failed and the zero-output initialization remained selected.
- [x] Preregister ADR 0079's fresh 32-game sealed test before ADR 0078
      validation is known; keep it unopened unless every validation gate
      passes.
- [x] Implement ADR 0079's conditional authorization, exact collector
      handoff, frozen external MLX evaluator, and bit-exact validation replay
      before any validation metric or test record exists.
- [x] Preregister ADR 0080's grouped Rust/MLX parity, latency, 20-game screen,
      and 100-game confirmation gates before ADR 0078 validation or ADR 0079
      test metrics are known.
- [x] Close ADR 0079 and ADR 0080 unopened after ADR 0078 validation failed;
      no test record, inference service, parity fixture, or gameplay seed was
      opened.
- [x] Promote only confirmed paired improvements.
- [x] Confirm positive late-terminal policy-improvement signal without
      weakening frozen mechanism guardrails.
- [x] Requalify the confidence-gated policy after canonical redetermination;
      demote it to explicit research after it failed the original non-Bear
      allocation guardrail.
- [x] Document the strongest factual result after the target was not reached:
      95.744 mean over 1,000 held-out games, 95% CI `[95.652,95.837]`, leaving
      a measured 4.256-point gap.

## Phase 7-8: Product Completion

- [x] Replace ad hoc cluster assignment with a manifest-backed research queue:
      dependency-aware claims, expiring leases, work stealing, host intent,
      deterministic priority, API/dashboard visibility, and dispatch through
      the existing host lock and `caffeinate`.
- [x] Meet versioned latency and throughput budgets for instant, interactive,
      research, and batch-32 MLX execution.
- [x] Profile and optimize the exact MLX champion rollout hot path with exact
      score/search parity; reduce representative R600 treatment time by 6.32%
      and define a progressive cluster experiment funnel.
- [x] Complete the full-terminal optimization pass without a learned leaf,
      rollout cutoff, reduced budget, or fallback. Confirm 96.345 mean over
      50 fresh R600 games, reach 3.88x projected throughput on one Mac and
      10.74x treatment throughput across john1, john2, and john3, with zero
      bootstrap samples or policy fallbacks.
- [x] Add and validate a 10x-plus R600 screening tier using two-focal-turn MLX
      bootstrapping, independent policy/leaf evaluators, explicit leaf timing,
      and three-node throughput evidence. Keep full terminal R600 mandatory
      for promotion after the screen measured a 3.688-point matched-seed gap
      and therefore sacrificed score.
- [x] Eliminate warning/debt backlog in v2. Compiler/lint debt is clean, the
      CLI and research monoliths are split by ownership, and structural tests
      prevent regression.
- [x] Complete documentation and one-command workflows, including generated
      CLI reference freshness checks.
- [x] Move v1 crates, historical scripts, and reports to the explicit
      `legacy/` reference boundary.
- [x] Rehearse a fresh clone on john3 through `make bootstrap`, `make setup`,
      and the complete `make check` gate with a clean final Git status.
- [x] Run 1,000-game held-out validation across all three local Macs and
      publish the final machine-readable and Markdown reports.

## Active Research Continuation

- [x] Complete R4's exact adaptive multi-resolution foundation across four
      unique corpus partitions. Exactness and adversarial gates passed, but
      HWF P99 reached 271/256 tokens at radius four and 298/288 at radius five;
      classify `r4_adaptive_multires_compactness_failed` and do not authorize
      MLX under ADR 0154.
- [x] Freeze the bounded radius-four far-summary successor with exact sidecar,
      deterministic wildlife/frontier quotient summaries, and joint token,
      scalar-width, byte, and throughput gates.
- [x] Implement and run ADR 0155's four independent bounded quotient arms over
      the identical 60,000-state corpus, one arm per Mac.
- [ ] Run ADR 0156's authorized matched MLX comparison of exact R2, Q1
      seat-marginal, Q2 directional, and Q3 habitat-affordance.
- [ ] Run R5 quotient-state exact-decoding controls and R6 incremental sparse
      apply/undo parity as independent cluster lanes.
- [ ] Complete ADR 0120's sole 200-epoch optimizer-schedule origin, clean
      replay, mixed ceiling, integration, and mechanical classification.
- [x] Use the three nontraining Macs for independent ADR 0121-0123
      generalization, attribution, and corruption-calibration decisions.
- [x] Preregister ADR 0124's contingent 50% local-geometry treatment without
      duplicating or changing ADR 0120.
- [x] Reject the first ADR 0124 preflight on frozen memory and overhead gates,
      then complete ADR 0125's narrow implementation repair with exact digest
      preservation.
- [ ] Mechanically authorize and launch one ADR 0124 origin only if ADR 0120
      is valid but insufficient; cancel it without training if ADR 0120 is
      sufficient.
