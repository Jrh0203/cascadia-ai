# Cascadia v3 Handoff — 2026-07-09

Operational snapshot captured at **2026-07-09 20:57 EDT** from the local
orchestrator and john0–john4. Live PIDs, counts, and ETAs will drift; re-run the
verification commands before acting. Scientific artifact identities and seed
domains are immutable.

## Executive truth

- The official rules engine now returns unplaced wildlife at end of turn and
  treats the free three-of-a-kind refresh as an explicit accept/decline policy
  decision. All forced-refresh baselines are historical only.
- The corrected-rules scalar cycle4 n1024/d16 baseline is complete and valid:
  **98.2975 mean seat score over 100 games**, P50 98, P90 102. This remains
  below the 100-point target.
- The matching corrected-rules distq-k8 n1024/d16 arm is the live decisive
  run. At this snapshot it had **39/100** complete games and an estimated
  completion around **02:59 EDT on July 10**.
- The n1024 raw-ledger watcher is dead. It missed scalar seed `2027070908`
  and exited before distq began. The scalar aggregate report is valid, but
  category attribution is blocked; live distq raw files are currently only in
  `/tmp` and require immediate preservation.
- The action-conditioned structured-value representation gate passed by
  reducing held-out RMSE 15.99%. Exact-grounded structured-Q schema v4,
  training, and the frozen-head verdict are implemented and queued on john0.
  There is no structured-Q model verdict or score claim yet.
- The fixed structured-Q pilot has 2,400 roots. A 12,000-root quarantined fit
  expansion passed its cross-shard audit. Three additional 1,600-root
  selection/verdict/replication reserves are complete per host and require the
  canonical combined harvest before use.
- Pairwise comparator serving and small-data/head-only candidate recall were
  killed by held-out offline gates before gameplay. Do not re-run them without
  materially new supervision or architecture.
- `main` was exactly synchronized with `origin/main` at parent revision
  `fbd0bf51ee15cd1c99bcf1976e891a0d47550560` before this handoff edit. The
  commit containing this handoff will necessarily have a later hash.

## Read order

1. `handoff-2026-07-09.md` — this timestamped operational snapshot.
2. `docs/v3/README.md` — canonical document index.
3. `docs/v3/CAMPAIGN_STATE.md` — live research state and RESUME HERE.
4. `cascadiav3/EXPERIMENT_LOG.md` — chronological evidence; focus on July 9.
5. `docs/v3/RESEARCH_LOG.md` — consolidated mechanisms and ranked directions.
6. `docs/v3/RULES_CONTRACT.md` — corrected rules identity and ordering.
7. `docs/v3/INFRASTRUCTURE.md` and `cascadiav3/README.md` — operations.
8. `docs/v3/RADICAL_DIRECTIONS.md`, `docs/v3/ARCHITECTURE.md`, and
   `docs/v3/TRAINING_PIPELINE.md` — architecture and methodology.

Treat current `main` and live artifacts as stronger than this snapshot. Do not
use the screenshots from the prior thread as live state.

## Repository and local-host state

Repository root: `/Users/johnherrick/cascadia`

Branch at capture: `main`

Parent revision at capture:
`fbd0bf51ee15cd1c99bcf1976e891a0d47550560`

The following unrelated operational paths were intentionally preserved and
must not be cleaned, reset, staged, or committed merely to obtain a clean
status:

```text
champion_server.sh
fleet2_gen.sh
fleet2_shard_john1_manifest.json
fleet3_gen.sh
fleet3_shard_john1_manifest.json
fleet4_gen.sh
fleet5_gen.pid
fleet5_gen.sh
venv/
webui.pid
webui_run.sh
```

The local machine is `Johns-Mac-mini.local`. The web/API service is alive at
`http://127.0.0.1:8787/`:

- API PID `65336`: `cascadia-api --listen 0.0.0.0:8787`
- suggestion exporter PID `68807`
- suggestion policy: distq-k8, n256/top16/d4, MPS, K16 interior
- `fleet5_gen.pid` points to dead PID `24105`; preserve it as unrelated state.

## Rules and compatibility boundary

Rules ID:

```text
cascadia_research_aaaaa_4p_card_a_no_habitat_bonus_rules_2026_07_09
```

Load-bearing corrections:

- Unplaced drafted wildlife returns to the bag before end-of-turn refill.
- A free three-of-a-kind wildlife refresh is optional.
- Automated policies choose decline/accept from public information.
- Accept search samples hidden replacements before ranking the revealed draft.
- Gumbel root and interior search use the same policy space.

Every old forced-refresh score, corpus, and checkpoint remains useful only as
historical architecture evidence. Promotion and the ≥100 claim require the
corrected rules identity.

## Scientific results that should guide the next session

### Corrected baselines

- Greedy: `87.5450` over 100 fresh seeds.
- Cycle4 no-search policy: `91.8425`; delta versus greedy `+4.2975`, 95% t-CI
  `[+3.8705,+4.7245]`.
- Cycle4 no-search Q: `90.8925`; delta `+3.3475`, CI
  `[+2.8507,+3.8443]`.
- Cycle4 n256/d4: `97.0675`.
- Distq-k8 n256/d4: `97.3075`; same-seed delta `+0.2400`, 95% t-CI
  `[-0.1139,+0.5939]`. Inconclusive; cycle4 retained.
- Cycle4 n1024/d16: `98.2975` over 100 games, P50 `98`, P90 `102`.
- Distq-k8 n1024/d16: live; owns the next strength decision.

### Architecture verdicts

- Pairwise comparator: learned pairs but failed routing. Pairwise Borda gained
  noise-sized top-1 hits while worsening mean completed-Q regret; additive
  routing was flat. Closed before gameplay.
- Exact full-menu candidate recall: incumbent best-action coverage was about
  86.1%. Soft imitation reduced coverage; direct recall training rescued only
  2/800 menus, produced no top-1 gain, and did not improve regret. Closed.
- Action-conditioned structured value: passed representation gate. Held-out
  RMSE `3.4889` versus best incumbent comparison `4.1528`, a 15.99% reduction.
- Exact K1: implementation and causal smoke passed. Final-turn exact decisions
  were 8.86x faster, but the two-seed score was flat and the 100-game CUDA gate
  remains queued.
- Smaller-model/larger-search: packed bridge inference is much faster, but
  trained S at equal-wall search lost too much score in the three-seed screen.
  CUDA packed throughput remains queued; do not distill XS from MPS bridge
  ratios alone.

### Performance verdicts

- MPS shared-batch knee is jobs2: jobs1→2 gave 1.147x wall throughput; jobs4
  reached only 1.180x and greatly increased latency.
- Dynamic seed scheduling removes fixed-chunk long tails while preserving
  actions/scores. The completed n256 trace predicted about 1.076x improvement.
- Parallel CPU leaf rollouts improve jobs1 interactive latency by 1.061x but
  are flat/slower with concurrent games. Keep opt-in for interactive use only.
- john0 showed roughly 64–66% mean GPU utilization with substantial CPU,
  memory, power, and thermal headroom. The jobs12/16/24 CUDA calibration is
  queued and must preserve action/score parity.

## john0 live state at 20:57 EDT

Host root: `/home/john0/cascadia`

| Role | PID | State |
|---|---:|---|
| corrected rebaseline shell | 1265148 | alive |
| total-verdict watcher | 1268022 | alive, waiting for main |
| n1024 raw-ledger watcher | 1284321 | **dead / failed** |
| post-chain waiter | 2241595 | alive, waiting |
| distq n1024 Python runner | 3556049 | alive |
| distq n1024 exporter | 3556050 | alive, ~784% CPU |
| distq CUDA bridge | 3556053 | alive, ~418% CPU |

Distq live command contract:

- seeds `2027070900..2027070999`
- n1024/top16/d16, depth 1
- eight optional-refresh samples
- blend 0.5, K16 interior
- jobs12, one shared CUDA bridge
- source `d20daf44dc6aa4aad3d03c6ccb7d3a21c3013135`
- manifest `cascadiav3/checkpoints/full_v3_distq_k8/best_locked_val.manifest.json`
- temporary raw directory `/tmp/tmphk9xcpuk/gumbel_batch`

At capture: 39 complete 81-row raw files; last-10 rate `10.06 games/hour`;
projected completion near `2026-07-10 02:59 EDT`.

## Corrected scalar n1024 artifact identities

Experiment ID: `rules_20260709_cycle4_n1024_d16`

```text
report SHA-256:
8c164dc6c05f34633b24c36fb28e9fd4234271670f523c8bd6d4e6abab953761

8,000-row decisions SHA-256:
d42cf655aa26facd2107887250ccb5e6cefab9a0d32260eb27df3419a3ce9922

exporter SHA-256:
89a48ce6d762295657d39c5faedaaed874a6ff3d2fc790ff5c3ac134610c1a29

cycle4 manifest SHA-256:
b8886c24cd93e19299e8c4cca4dd7671fe16b685d54949de014d6f9d5aee616d

cycle4 weights SHA-256:
33559aab05324e74998164d4e59e7adec9fa3c77da531dd4797c718cf4cfd354
```

Distq artifacts:

```text
manifest SHA-256:
02fa7ccab88e2313363882d5251d9b44ae364a05eb23f4045725803da9bd6533

weights SHA-256:
8d0272c971bcaae407fd23f3f47daae6fa50d8326a4af76243046c038c041f40
```

## Critical blocker: n1024 category ledgers

Watcher log:

```text
[raw-ledger] monitoring tag=rules_20260709_cycle4_n1024_d16 python_pid=1739796
missing raw game file: /home/john0/cascadia/cascadiav3/reports/rules_20260709_cycle4_n1024_d16_raw_games/gumbel_game_seed_2027070908.jsonl
```

Facts:

- 99 scalar raw game files are durable and valid.
- Missing scalar seed: `2027070908` only.
- The scalar temporary batch directory is gone.
- The aggregate report pins seed 0908 seat scores to `97 / 98 / 97 / 100`,
  mean `98.0`.
- All 80 scalar decision rows for seed 0908 exist.
- The category arrays exist only in the missing game-done row.
- The dead watcher never began monitoring distq.
- The distq raw directory is temporary and can disappear when PID `3556049`
  exits.

### Immediate preservation command

Before doing analysis, preserve the currently complete distq raw files. This
does not publish a ledger or inspect scores; it only copies complete per-seed
files into the intended durable mirror:

```bash
ssh john0 /bin/bash -s <<'REMOTE'
set -euo pipefail
cd "$HOME/cascadia"
src=/tmp/tmphk9xcpuk/gumbel_batch
dest=cascadiav3/reports/rules_20260709_distq_k8_n1024_d16_raw_games
test -d "$src"
mkdir -p "$dest"
find "$src" -maxdepth 1 -type f -name 'gumbel_game_seed_*.jsonl' \
  -exec cp -f {} "$dest"/ \;
printf 'mirrored='; find "$dest" -maxdepth 1 -type f \
  -name 'gumbel_game_seed_*.jsonl' | wc -l
REMOTE
```

Then install a replacement distq-only copy loop or repeat the copy until the
runner exits. Do not reuse the failed watcher's PID file or claim completion
until all 100 files contain exactly 81 rows and one game-done row each.

### Scalar seed-0908 recovery contract

Do not synthesize categories from totals. After distq finishes, replay exactly
one scalar seed under the same source, binary, checkpoint, rules, search, and
TF32-off execution contract. The replay is admissible only if:

1. all 80 chosen-action IDs match the existing decision ledger;
2. all optional-refresh decisions match;
3. all four seat totals equal `97 / 98 / 97 / 100`;
4. category components sum exactly to those totals;
5. the replayed row is inserted at seed order 0908;
6. the complete 100-row scalar ledger passes `compare_game_categories` input
   validation before any mechanism claim.

The running john0 source is still d20. The post-chain waiter will replace it
with f35 immediately after the main and verdict watchers exit. Coordinate the
replay with that transition; do not accidentally replay on f35 and label it
d20.

## Automatic post-chain behavior

Waiter PID: `2241595`

Script: `/tmp/postchain-waiter-main-f35b0d0b.sh`

Pinned archive:

```text
/tmp/cascadia-main-postchain-f35b0d0b.tar.gz
SHA-256 460857f26f7431727db623313f92df2e5be13a27033bd72d642eb6d650fc7a81
```

It waits until both PIDs `1265148` and `1268022` exit, verifies the archive
and the three pilot NPZ hashes, deploys source
`f35b0d0b209444f8c09e7e603c380f1d8edbc100`, and runs strictly sequentially:

1. 100-seed exact-K1 corrected n256 gate;
2. frozen structured-Q head pilot and untouched verdict;
3. CUDA packed model-size throughput probe;
4. sample-8 versus sample-4 100-seed gate;
5. jobs12/16/24 CUDA concurrency calibration.

A scientific structured-Q failure returns control to later engineering gates;
a crash or malformed verdict stops the shell. The dead raw watcher does not
block this sequence. If category recovery must precede source replacement,
the next operator must explicitly coordinate or pause this waiter before the
main/verdict PIDs exit.

## Structured-Q artifacts

### Fixed pilot staged on john0

| Role | Seeds | Roots | NPZ SHA-256 |
|---|---|---:|---|
| fit | 2027073500..09 | 800 | `06d550b4b70b32bab1e7bea4d994a26341d7b5a8d3dc58b8fc636d89c31e8519` |
| LR selection | 2027073510..19 | 800 | `5095d572b2167f81931d2a5ba7d8a339ffba285d04dd3ff55eeac99c647688cc` |
| untouched verdict | 2027073520..29 | 800 | `cdbd54b0c2aaa79fbc4c1a12c73eaae863d3a62ab8924a8173df12c9717eb6b4` |

Frozen-head gate:

- candidate selected-final RMSE must be `<=3.1968`;
- paired absolute-error t-CI must be wholly below zero;
- all-Q RMSE must be `<=1.8374`;
- mean completed-Q regret must be `<=0.8015`;
- exact K1 rows are excluded from the primary read;
- LR selection and verdict seed blocks are disjoint.

### Quarantined fit expansion, locally harvested

| Block | Seeds | Roots | Actions | NPZ SHA-256 |
|---|---|---:|---:|---|
| expansion A | 2027073600..49 | 4,000 | 1,771,087 | `225aeff6dd73e0902b1786d09f8236e7e8c53301beda82441b70c05d0429e74f` |
| expansion B | 2027073650..99 | 4,000 | 1,791,934 | `0447d69bff7bef39261ef8cfd09bb37e6cbf2bf5545ac82eff3516615776b69a` |
| expansion C | 2027073700..49 | 4,000 | 1,736,266 | `5dc0860dca996a719d87f2aae33f88e50238bbb37e45536dce96380e3d041959` |

Cross-shard audit SHA-256:
`e1edbad3552abef2321808666948f299fbf3ba226b948d50a2314b696fb5eb14`

Audit totals: 150 seeds, 12,000 roots, 5,299,287 actions, 46,200 q-valid
actions, 600 exact rows, max Q-identity error `3.8146973e-6`, zero component-
sum errors. It remains fit inventory only and may not enter training unless
the fixed frozen-head pilot passes.

### Candidate-blind reserves, complete remotely

| Role / host | Seeds | Roots | Actions | NPZ SHA-256 |
|---|---|---:|---:|---|
| selection / john2 | 2027073750..69 | 1,600 | 711,027 | `48e48e74f0853c434d4ec157d188b092463676d40c1b389059f9a2dca86ad46d` |
| verdict / john3 | 2027073770..89 | 1,600 | 667,699 | `99b85671881ae6bde5d49c1a07588b27b368fe3194450aea52e223ed54b668d8` |
| replication / john4 | 2027073790..3809 | 1,600 | 680,007 | `41b5bd6098c96b05ab6a14e1a53042ae71104864e111e78bbc6f645b5caab5a7` |

All per-host summaries and invariants pass. Run the canonical harvest from the
orchestrator:

```bash
cascadiav3/scripts/fetch_structured_q_reserve_holdouts.sh
```

That script rechecks remote/local hashes, exact seed roles, one shared
contract, and disjointness from all six pilot/expansion shards. It performs no
john0 or training copy.

## Immediate resume checklist

1. Verify current Git and preserve unrelated untracked files:

   ```bash
   cd /Users/johnherrick/cascadia
   git fetch origin main
   git rev-parse HEAD
   git rev-parse origin/main
   git status --short
   ```

2. Recheck john0 PIDs and distq progress; do not trust this timestamp:

   ```bash
   ssh john0 'cd "$HOME/cascadia"; ps -o pid=,ppid=,etime=,%cpu=,%mem=,args= \
     -p 1265148,1268022,2241595,3556049,3556050,3556053'
   ```

3. **Urgently preserve live distq raw files** using the command in the
   category-ledger section, then keep the mirror current through process exit.

4. Decide whether category-ledger recovery must precede automatic post-chain
   deployment. If yes, coordinate PID `2241595` before the main/verdict PIDs
   exit. Do not race source replacement.

5. Harvest and globally audit the completed reserve holdouts:

   ```bash
   cascadiav3/scripts/fetch_structured_q_reserve_holdouts.sh
   ```

6. After distq completes, verify the aggregate total verdict. Do not run the
   final category harvest until both 100-row category ledgers exist:

   ```bash
   cascadiav3/scripts/fetch_rules_n1024_verdict.sh
   ```

   This command is expected to fail closed while the category artifacts are
   incomplete; that refusal is correct.

7. Recover scalar seed 0908 under the exact d20 contract and validate it as
   specified above. Then rebuild both category summaries and reconcile the
   category total with the canonical total verdict field-for-field.

8. Observe the automatic john0 chain. Require reports, manifests, ledgers, and
   verdicts; a busy process alone is not evidence.

9. Update `CAMPAIGN_STATE.md`, `RESEARCH_LOG.md`, and `EXPERIMENT_LOG.md` at
   every material transition. Do not allow the durable Markdown to lag live
   conclusions again.

## Commands for routine verification

Python suite:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=cascadiav3/src \
  venv/bin/python -m unittest discover -s cascadiav3/tests -v
```

Rust exporter:

```bash
cargo test --manifest-path cascadiav3/real-root-exporter/Cargo.toml
```

Workspace:

```bash
cargo check --workspace
cargo test --workspace
git diff --check
```

## Do not do

- Do not use forced-refresh results as promotion controls.
- Do not inspect partial distq scores or adapt the experiment to them.
- Do not invent the missing scalar category row from its known totals.
- Do not fold expansion or reserve shards into training automatically.
- Do not run Mac-fleet data as a promotion gate.
- Do not run concurrent john0 scientific jobs.
- Do not enable TF32 for batteries.
- Do not re-propose pairwise serving, head-only candidate recall, oracle/belief
  modeling, ensembles, symmetry TTA, leaf softmix, serving table-total,
  chance-node expectimax, or more scalar scaling without new evidence.
- Do not clean or stage unrelated untracked operational files.
- Do not treat validation loss, a smoke score, process activity, or an
  incomplete ledger as a strength result.

## Definition of the next successful handoff

The next handoff should contain, at minimum:

- the final corrected scalar-versus-distq n1024 total verdict;
- both complete category ledgers and the reconciled mechanism verdict, or an
  explicit durable account of why recovery failed;
- the exact-K1 100-game verdict;
- the structured-Q frozen-head verdict and selected LR/checkpoint hashes;
- the CUDA packed throughput, market-sample, and concurrency verdicts if run;
- updated john0 queue/PIDs and all new artifact SHA-256 values;
- explicit confirmation that `HEAD == origin/main` and the worktree contains
  only the known unrelated operational paths.
