# Handoff — 2026-07-23

Current source of truth is `main`; verify `HEAD == origin/main`.
Read `docs/v3/README.md` and `docs/v3/CAMPAIGN_STATE.md` before acting.

## Live pure-wildlife job

The cap-six all-rules bounded-maximization top-frontier pass is active under
tag `all_wildlife_bound_probe_topfrontier435_20260723`. It covers all 435
current count ties at the ruleset sound frontier above score 85, spanning 280
rulesets. Inputs and launch state are pinned in
`cascadiav3/fleet/all_wildlife_bound_probe_topfrontier435_fleet_20260723.json`.

| Host | Tasks | Wrapper PID | Start UTC | Launcher |
|---|---:|---:|---|---|
| john1 | 109 (`index % 4 == 0`) | 63770 | 22:44:57 | `screen:wildlife_bound_topfrontier435_john1` |
| john2 | 109 (`index % 4 == 1`) | 78279 | 22:44:57 | `nohup` |
| john3 | 109 (`index % 4 == 2`) | 36045 | 22:46:21 | `screen:wildlife_bound_topfrontier435_john3` |
| john4 | 108 (`index % 4 == 3`) | 36561 | 22:46:21 | `screen:wildlife_bound_topfrontier435_john4` |

All four had current five-second heartbeats at 22:47 UTC. The hard terminal
deadline is `2026-07-24T08:46:21Z`. Do not kill or restart any worker.

Results are sealed until all four files below exist:

```text
~/cascadia/cascadiav3/logs/all_wildlife_bound_all_wildlife_bound_probe_topfrontier435_20260723_john1.exit
~/cascadia/cascadiav3/logs/all_wildlife_bound_all_wildlife_bound_probe_topfrontier435_20260723_john2.exit
~/cascadia/cascadiav3/logs/all_wildlife_bound_all_wildlife_bound_probe_topfrontier435_20260723_john3.exit
~/cascadia/cascadiav3/logs/all_wildlife_bound_all_wildlife_bound_probe_topfrontier435_20260723_john4.exit
```

john1 is local; query the other paths through `ssh john2`, `ssh john3`, and
`ssh john4`. Heartbeat files use the same basename with `.heartbeat`.

The first john2 `nohup` launch left its invoking SSH shell attached despite
redirected streams. The worker itself is healthy and durable. That launcher
may later attempt already-launched john3/john4 commands, but each worker fails
closed on its existing single-use PID/exit files, so it cannot duplicate
scientific work. Do not interrupt it while john2 is live.

A continuation through John's 09:00 EDT window is frozen and queued:
`cascadiav3/fleet/all_wildlife_bound_probe_secondfrontier224_taskset_20260723.json`,
SHA
`5c7221f6fb63c8d4777a8d68d4f93bfe86c78c3c9ca63f1c39bef64ee0c2a8c0`.
It contains 224 disjoint layer-two frontier cases across 113 rulesets,
56/host. Its durable waiter is live in screen
`wildlife_bound_secondfrontier224_waiter` (PID 82731), heartbeat
`cascadiav3/logs/queue_all_wildlife_bound_secondfrontier224.heartbeat`. It
requires four zero predecessor exits and must pass its HOLD/hash/collision
checks before launching. Do not read first-pass task JSON to decide whether
to launch it; the continuation is already fixed.

**2026-07-24 correction:** the waiter launched only john1. john1 completed 56
tasks; john2–4 created no worker or output, and the receipt incorrectly
accepted blank PIDs. Do not use the old waiter again. The exact 168 missing
cases are frozen in
`cascadiav3/fleet/all_wildlife_bound_probe_secondfrontier168_recovery_taskset_20260724.json`
(SHA
`516ecf0b7737a1d2f9cf2bd5e949d03bda2f1461a41492b1d70fe52b95748017`)
for a 42/host recovery. Continuation outputs remain sealed until that recovery
is terminal.

## Pinned identities

- Base bound catalog:
  `docs/v3/evidence/all_wildlife_catalog_bound_probe_pilot8_2026-07-23.json`,
  SHA-256
  `e6927839a3cabdaf5d6316fc5a7e8641940d5c123406bac359376a0a3f598b82`
- Taskset:
  `cascadiav3/fleet/all_wildlife_bound_probe_topfrontier_taskset_20260723.json`,
  SHA-256
  `623cb660909c8e0d86e46c8a5776f4e567d9a573c760252505733508ab844759`
- Probe:
  `4c5047c99370d9a433f0b448fe31db5a1dda85fa4b4519aa52498990b788fe31`
- Exact model:
  `c5667b9d24c79489dd916c9e2a1548c08922d5f36783e67fac53fe7093add5e9`
- Exact support:
  `362b5d7f82a156579e33c4b2c630c06bff3f45fa08f72a4dc70fe378eadca329`
- Rules/scorer:
  `c94945c6c2865e6566697edf8de233fa91763e73d6b2be185cd53c23d4e63790`
- Worker:
  `c7dcc31a7bd7d55016d470ebb04112341766cb050a39811d0f8c40aa777008e4`

Configuration is connected exact maximization, 300 seconds/task, 330 seconds
total/task, eight CP-SAT workers, and five-second heartbeats.

## Resume checklist after all four terminal markers

1. Require all four first-pass exit values to be zero. The active waiter
   should then launch the continuation automatically; do not launch a
   duplicate. Confirm its `.launched` receipt and four new worker heartbeats.
2. Keep the continuation's results sealed until its own four exit files
   exist and equal zero. The first-pass results may be collected after the
   continuation launch because the second taskset is already frozen, but do
   not adapt or replace the continuation from them.
3. Rsync each host's
   `cascadiav3/fleet_outputs/all_wildlife_bound_probe_topfrontier435_20260723/`
   and
   `cascadiav3/fleet_outputs/all_wildlife_bound_probe_secondfrontier224_20260723/`
   into distinct per-tag/per-host collection directories.
4. Validate exactly 435 + 224 unique, mutually disjoint task identities and
   every pinned source/base hash. Never treat `UNKNOWN` as exact.
5. Run `tools/all_wildlife_bound_probe_collect.py` against the pinned base
   catalog and all eight tag/host collection directories, with
   `target/release/all_wildlife_score_oracle`. The collector must preserve
   inherited `unresolved_count_upper_bounds` and production-rescore all 1,024
   stored boards.
6. Apply the frozen next-round rule: repeat a top-frontier round only if at
   least 109/435 selected counts strictly contract or any verified witness
   improves a ruleset incumbent. Retain every valid contraction, closure, and
   witness under either verdict.
7. Update both fleet ledgers, `cascadiav3/EXPERIMENT_LOG.md`,
   `docs/v3/README.md`, `docs/v3/CAMPAIGN_STATE.md`, and
   `docs/v3/RESEARCH_LOG.md`; commit and push.

## Current catalog state

Before the live pass, 80/1,024 rulesets are certified optimal. Every ruleset
has a validated connected 20-token cap-six board, but the other 944 boards
remain explicitly labeled incumbents. The holistic incumbent is 85 on eight
rulesets and the sound global upper is 99.

The eight-case pilot that selected this run is complete and curated at
`docs/v3/evidence/all_wildlife_bound_probe_pilot8_2026-07-23.json`. It
contracted CBDDB/DDDDB 99→98 and DBDAB/DADAB 94→92, found no new witness,
and preserved the all-1,024 production response SHA
`5f3b7b5dcc0fee92b766e59b3f99b5b1a25b8afd5f38431a0264b64809c251d4`.
