# Handoff — 2026-07-12 (~01:30 EDT)

Timestamped snapshot; current `main` and live artifacts outrank this file.
Read `docs/v3/README.md` → `docs/v3/CAMPAIGN_STATE.md` (RESUME HERE points
at the 07-12 addendum at the file's end) first.

## Live on john0 (all session-independent, chained, self-gating)

| Chain | PID file | What it does |
|---|---|---|
| `screen_wave_20260712` | `cascadiav3/logs/screen_wave_20260712.pid` | RUNNING: combo screen + R1.3a coverage audit remain (ghost/qbias/lcb screens done) |
| `refresh_gate_20260712` | `...refresh_gate_20260712.pid` | waits for wave → preregistered R0.6(i) gate, 100 seeds `2027072300..99` |
| `ghost_gate_20260712` | `...ghost_gate_20260712.pid` | waits for refresh gate → **the headline**: n512-ghost vs n256 champion, 100 seeds `2027072400..99` |

Pause files: `HOLD_experiment_queue` (between queue stages),
`HOLD_paired_gate` (between gate arms). Deployed revision on john0:
`e252d68e` (marker `cascadiav3/logs/exact_k1_deployed_revision.txt`);
`run_paired_gate.sh` + queue templates shipped separately (orchestration
scripts, not scored code).

## Results already in (see EXPERIMENT_LOG 07-11 → 07-12)

- R0.1 sigma CLOSED (confirm null); R0.2 CLOSED (secondary CI+ noted);
  R1.1a: no cheap contention; concurrency: jobs12 retained; R3.6:
  DECELERATING (+0.21 vs +0.615 predicted) → portfolio reweighted to
  structural programs.
- R2.1 bank ACCEPTED (incumbent regret 0.2351; instrument cross-checks
  gate truth). Screens now cost ~6 min.
- **Ghost screen PASS: +0.0074 bias vs +0.020 bar** — R1.2A's gate armed.
- Q-bias screen: structurally null at n256 (all top-m visited; value moves
  to training targets / R1.4). LCB screen: flat (-0.0002).

## Morning checklist

1. `ssh john0 'bash /home/john0/cascadia/cascadiav3/scripts/morning_report.sh'`
   — one file with every verdict.
2. Ghost gate CI+ at ≤1.25x wall → launch the n1024-tier confirmation
   (template stage in `cascadiav3/queues/queue_20260712_gates_template.jsonl`,
   block `2027072600..99` registered; complete its preregistration entry
   first — set CAND_N from the gate's timing block).
3. Refresh gate CI floor above -0.25 + wall saving → adopt divisor 4 as
   serving default (update README standing contracts).
4. Coverage audit: drop rate <1% and regret <0.01 → close R1.3a-c per
   preregistration.
5. Next builds (deliberately not scaffolded): R1.4 densification (multi-day,
   needs corpus regen design), R2.4 bridge throughput (interactive on john0).

## How to run anything

- Screen any flag combo (~6 min): `SCREEN_NAME=x EXTRA_FLAGS="..."
  SOURCE_REVISION=<marker> bash cascadiav3/scripts/run_bank_screen.sh`
- Gate any candidate: `run_paired_gate.sh` (env-parameterized; register a
  fresh seed block + preregister first)
- Sequence experiments: JSONL in `cascadiav3/queues/` +
  `run_experiment_queue.sh` (see `cascadiav3/queues/README.md`)
