# Handoff — 2026-07-10 (14:00 EDT snapshot)

Timestamped snapshot; **current `main` and live artifacts outrank this
file.** Start at [docs/v3/README.md](../v3/README.md), then
[CAMPAIGN_STATE.md](../v3/CAMPAIGN_STATE.md) RESUME HERE.

## What closed today

- **Corrected-rules rebaseline: fully closed.** distq ties cycle4 at
  n1024/d16 (`98.3850` vs `98.2975`, ns) — cycle4 scalar retained champion.
  Both lost raw seeds replayed bit-exact and installed (ledgers 100/100);
  category attribution flat in every category; canonical harvest in
  `cascadiav3/reports/rules_20260709_rebaseline_complete/` (orchestrator).
- **Exact-K1 ADOPTED** (John's ruling: declared one-seed exclusion): flat
  `-0.0379` CI `[-0.0859,+0.0101]` on 99 causally-valid pairs, `28.99x`
  exact-frontier speedup. `--gumbel-exact-endgame-turns 1` is the serving
  default. **Exact K2 closed by ruling** — deeper plies stay on model
  inference.
- **Structured-Q head pilot FAILED its kill test** (selected-final RMSE
  `4.1573` vs threshold `3.1968`; paired CI wrong side of zero). Direction
  closed; 12,000-root expansion + reserves stay quarantined.
- **Small-model serving closed on CUDA**: S only `1.9x` M on the 5090.
- **Market sample-4 gate FAILED preregistration** (CI floor `-0.4684` <
  `-0.25` margin despite `1.575x` speedup) — sample-8 stays. Its
  comparator's trace-frontier premise was invalid for this knob; fixed to
  verdict on score+speedup only.
- **Category profile analysis**: the ~1.7-point gap to 100 is diffuse (no
  seat below 90; 31% of seats ≥100); bear-zeroing is near-cost-free triage,
  not a leak; high-budget scaling gains concentrate in habitat.

## Live on john0 (all session-independent, heartbeat-logged)

| Job | File under `cascadiav3/logs/` | Pause file |
|---|---|---|
| GPU chain: worlds screen det4→det8, then jobs12/16/24 concurrency probe | `gpu_autochain.{sh,log,pid}` | `HOLD_gpu_autochain` |
| Verdict-gated confirmation: computes screen verdict, launches n1024 det16/det32 (~20h) ONLY if CI+ | `worlds_confirm_waiter.{sh,log,pid}` | `HOLD_worlds_confirm` |

Screen: cycle4 scalar, n256/top16, K1 on, s8, det4 vs det8, fresh block
`2027071500..1599` (preregistered EXPERIMENT_LOG 07-10 11:20; decision rule
fixed before launch: CI+ → confirmation on reserved block `2027071600..1699`;
otherwise worlds axis closes at low budget with no further spend). Verdict
artifacts will be `worlds_screen_20260710_n256_verdict.{json,md}` on john0.
Portable stdlib tooling for on-box validation/verdicts lives in
`cascadiav3/logs/replay_tools/`.

## Rules of engagement changes (AGENTS.md)

- **Autonomy boundary added 07-10**: the agent launches preregistered
  gates/screens/probes/generation — and, at its discretion, training
  lines — without asking. Reserved to John: champion promotion, deliberate
  rules design changes, methodology rulings on blocked verdicts, spending
  money, destructive actions. Rules *bugs* are fixable with loud notice.
- Loop cadence: 59-minute wakeups + event monitor.

## Resume checklist for a fresh session

1. `bash cascadiav3/scripts/campaign_status.sh` — verify live state.
2. Read `gpu_autochain.log` and `worlds_confirm_waiter.log` tails on john0;
   every stage announces itself and failures say FAILED loudly.
3. If the screen verdict exists: log it (EXPERIMENT_LOG + RESEARCH_LOG §5
   item 7 + README status), and verify the confirmation launched iff CI+.
4. If the confirmation ran overnight: its verdict decides whether a
   champion-config change (more worlds) is proposed — **promotion remains
   John's call**.
5. Concurrency probe report (`cuda_concurrency_20260709_n64_d4*`): log the
   jobs12/16/24 throughput/divergence trade; it sets gate concurrency
   policy.
6. Open docket beyond worlds: see RESEARCH_LOG §5 (items 4, 6 parked; most
   directions now closed — if worlds is flat, the next conversation is
   training-side signal, with John).
